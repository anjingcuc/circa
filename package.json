{
  "name": "circa",
  "version": "0.1.0",
  "description": "CIRCA (CIRcular CAmera) — a tiny always-on-top circular webcam overlay",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
